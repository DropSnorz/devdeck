/** localStorage key the dashboard's zustand store persists under. Shared
 * with the M5 share/URL flow, which reads/writes the same store. */
export const DASHBOARD_STORAGE_KEY = 'devdeck.dashboard'
export const DASHBOARD_STORAGE_VERSION = 1
